[project]
name = "my-simple-day-os"
version = "0.1.0"
description = "My simple day OS - A simple operating system for managing my day to day while keeping things stupid simple."
readme = "README.md"
requires-python = "==3.12.0"
dependencies = [
    "ruff>=0.16.0",
]
